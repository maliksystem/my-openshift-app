apiVersion: v1
kind: Service
metadata:
  name: my-openshift-app-svc
  namespace: default
spec:
  selector:
    app: my-openshift-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
